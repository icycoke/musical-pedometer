<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">

    <Button
            android:id="@+id/startOrStopButton"
            android:layout_width="219dp"
            android:layout_height="54dp"
            android:layout_marginBottom="24dp"
            android:onClick="startOrStopOnClick"
            android:text="@string/start"
            app:layout_constraintBottom_toTopOf="@+id/downloadReportButton"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

    <Button
            android:id="@+id/downloadReportButton"
            android:layout_width="219dp"
            android:layout_height="54dp"
            android:layout_marginBottom="48dp"
            android:text="@string/download_report"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

    <fragment
            android:id="@+id/mapFragment"
            android:name="com.icycoke.musicalpedometer.MapsFragment"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toTopOf="@+id/startOrStopButton"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>